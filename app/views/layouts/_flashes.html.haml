-#.row
-#  .col-sm-12
-#    &nbsp;
-#.row
-#  .col-sm-12
-#    - flash.each do |key, value|
-#      .alert.alert-dismissible{ class: "alert-#{flash_to_css_class_mapping(key)}" }
-#        = sanitize(value)
-#        %button.close{'aria-label': 'Close', 'data-dismiss': 'alert', type: 'button'}
-#          %span{'aria-hidden': true} ×
-#
-#    = yield :flashes

- content_for :scripts do
  :coffee
    $(document).ready () ->
      flashToast(#{flash.to_json.html_safe})


